For a given `raw_story` and `update_instruction` you need to output relevant set of `storyPatches` as per specified schema.

Typically, just add to the story and/or modify facts. Do NOT reformat or re-arrange the story unnecessarily. 
